/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita-swift
 */
import Foundation
import BeetSolana
import Beet
import Solana


/**
* Arguments used to create {@link Editionmarker}
* @category Accounts
* @category generated
*/
public protocol EditionmarkerArgs {
    var editionMarkerDiscriminator: [UInt8] { get }
     var key: Key { get }
     var ledger: [UInt8] /* size: 31 */ { get }
}

/**
 * Holds the data for the {@link Editionmarker} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
public struct Editionmarker: EditionmarkerArgs {
  public static let editionMarkerDiscriminator = [233, 10, 18, 230, 129, 172, 37, 234] as [UInt8]

  public let editionMarkerDiscriminator: [UInt8]
  public let key: Key
  public let ledger: [UInt8] /* size: 31 */

  /**
   * Creates a {@link Editionmarker} instance from the provided args.
   */
  public static func fromArgs(args: Args) -> Editionmarker {
    return Editionmarker(
        editionMarkerDiscriminator: args["accountDiscriminator"] as! [UInt8],
        key: args["key"] as! Key,
        ledger: args["ledger"] as! [UInt8] /* size: 31 */
    )
  }
  /**
   * Deserializes the {@link Editionmarker} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  public static func fromAccountInfo(
    accountInfo: Foundation.Data,
    offset:Int=0
  ) -> ( Editionmarker, Int )  {
    return Editionmarker.deserialize(buf: accountInfo, offset: offset)
  }
  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Editionmarker} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  public static func fromAccountAddress(
    connection: Api,
    address: PublicKey,
    onComplete: @escaping (Result<Editionmarker, Error>) -> Void
  ) {
    connection.getAccountInfo(account: address.base58EncodedString) { result in
        switch result {
            case .success(let pureData):
                if let data = pureData.data?.value {
                    onComplete(.success(Editionmarker.deserialize(buf: data).0))
                } else {
                    onComplete(.failure(SolanaError.nullValue))
                }
            case .failure(let error):
                onComplete(.failure(error))
        }
    }
  }
  /**
   * Deserializes the {@link Editionmarker} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  public static func deserialize(
    buf: Foundation.Data,
    offset: Int = 0
  ) -> ( Editionmarker, Int ) {
    return editionMarkerBeet.deserialize(buffer: buf, offset: offset)
  }
  /**
   * Serializes the {@link Editionmarker} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  public func serialize() -> ( Foundation.Data, Int ) {
    return editionMarkerBeet.serialize(instance: Editionmarker(editionMarkerDiscriminator : self.editionMarkerDiscriminator,
        key : self.key,
        ledger : self.ledger))
  }
    /**
  * Returns the byteSize of a {@link Buffer} holding the serialized data of
  * {@link Editionmarker}
  */
  static func byteSize() -> UInt {
      return editionMarkerBeet.byteSize
  }
  /**
  * Fetches the minimum balance needed to exempt an account holding
  * {@link Editionmarker} data from rent
  *
  * @param connection used to retrieve the rent exemption information
  */
  static func getMinimumBalanceForRentExemption(
      connection: Api,
      commitment: Commitment?,
      onComplete: @escaping(Result<UInt64, Error>) -> Void
  ) {
      return connection.getMinimumBalanceForRentExemption(dataLength: UInt64(Editionmarker.byteSize()), commitment: commitment, onComplete: onComplete)
  }
  /**
  * Determines if the provided {@link Buffer} has the correct byte size to
  * hold {@link Editionmarker} data.
  */
  static func hasCorrectByteSize(buf: Foundation.Data, offset:Int=0) -> Bool {
      return buf.bytes.count - offset == Editionmarker.byteSize()
  }
}
  /**
   * @category Accounts
   * @category generated
   */
  public let editionMarkerBeet = BeetStruct(
    fields:[
        ("accountDiscriminator", (.init(value: .collection(UniformFixedSizeArray<UInt8>(element: .init(value: .scalar(u8())), len: 8))))),
        ("key", keyBeet),
        ("ledger", (.init(value: .collection(UniformFixedSizeArray<UInt8>(element: .init(value: .scalar(u8())), len: 31)))))
    ],
    construct: Editionmarker.fromArgs,
    description: "Editionmarker"
)
