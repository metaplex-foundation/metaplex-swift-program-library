/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita-swift
 */
import Foundation
import Solana
import Beet

/**
 * @category Instructions
 * @category Transfer
 * @category generated
 */
public struct TransferInstructionArgs{
    let instructionDiscriminator: [UInt8] /* size: 8 */
    let transferArgs: TransferArgs

    public init(
        instructionDiscriminator: [UInt8] /* size: 8 */ = transferInstructionDiscriminator,
        transferArgs: TransferArgs
    ) {
        self.instructionDiscriminator = instructionDiscriminator
        self.transferArgs = transferArgs
    }
}
/**
 * @category Instructions
 * @category Transfer
 * @category generated
 */
public let TransferStruct = FixableBeetArgsStruct<TransferInstructionArgs>(
    fields: [
        ("instructionDiscriminator", Beet.fixedBeet(.init(value: .collection(UniformFixedSizeArray<UInt8>(element: .init(value: .scalar(u8())), len: 8))))),
        ("transferArgs", transferArgsBeetWrapped)
    ],
    description: "TransferInstructionArgs"
)
/**
* Accounts required by the _Transfer_ instruction
*
* @property [_writable_] token  
* @property [] tokenOwner  
* @property [_writable_] destination  
* @property [] destinationOwner  
* @property [] mint  
* @property [_writable_] metadata  
* @property [] edition (optional)  
* @property [_writable_] ownerTokenRecord (optional)  
* @property [_writable_] destinationTokenRecord (optional)  
* @property [**signer**] authority  
* @property [_writable_, **signer**] payer  
* @property [] sysvarInstructions  
* @property [] splTokenProgram  
* @property [] splAtaProgram  
* @property [] authorizationRulesProgram (optional)  
* @property [] authorizationRules (optional)   
* @category Instructions
* @category Transfer
* @category generated
*/
public struct TransferInstructionAccounts {
    let token: PublicKey
    let tokenOwner: PublicKey
    let destination: PublicKey
    let destinationOwner: PublicKey
    let mint: PublicKey
    let metadata: PublicKey
    let edition: PublicKey?
    let ownerTokenRecord: PublicKey?
    let destinationTokenRecord: PublicKey?
    let authority: PublicKey
    let payer: PublicKey
    let systemProgram: PublicKey?
    let sysvarInstructions: PublicKey
    let splTokenProgram: PublicKey
    let splAtaProgram: PublicKey
    let authorizationRulesProgram: PublicKey?
    let authorizationRules: PublicKey?

    public init(
        token: PublicKey,
        tokenOwner: PublicKey,
        destination: PublicKey,
        destinationOwner: PublicKey,
        mint: PublicKey,
        metadata: PublicKey,
        edition: PublicKey? = nil,
        ownerTokenRecord: PublicKey? = nil,
        destinationTokenRecord: PublicKey? = nil,
        authority: PublicKey,
        payer: PublicKey,
        systemProgram: PublicKey? = nil,
        sysvarInstructions: PublicKey,
        splTokenProgram: PublicKey,
        splAtaProgram: PublicKey,
        authorizationRulesProgram: PublicKey? = nil,
        authorizationRules: PublicKey? = nil
    ) {
        self.token = token
        self.tokenOwner = tokenOwner
        self.destination = destination
        self.destinationOwner = destinationOwner
        self.mint = mint
        self.metadata = metadata
        self.edition = edition
        self.ownerTokenRecord = ownerTokenRecord
        self.destinationTokenRecord = destinationTokenRecord
        self.authority = authority
        self.payer = payer
        self.systemProgram = systemProgram
        self.sysvarInstructions = sysvarInstructions
        self.splTokenProgram = splTokenProgram
        self.splAtaProgram = splAtaProgram
        self.authorizationRulesProgram = authorizationRulesProgram
        self.authorizationRules = authorizationRules
    }
}

public let transferInstructionDiscriminator = [163, 52, 200, 231, 140, 3, 69, 186] as [UInt8]

/**
* Creates a _Transfer_ instruction.
*
* @param accounts that will be accessed while the instruction is processed
  * @param args to provide as instruction data to the program
 * 
* @category Instructions
* @category Transfer
* @category generated
*/
public func createTransferInstruction(accounts: TransferInstructionAccounts, 
args: TransferInstructionArgs, programId: PublicKey=PublicKey(string: "metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s")!) -> TransactionInstruction {

    let data = TransferStruct.serialize(
            instance: ["instructionDiscriminator": transferInstructionDiscriminator,
"transferArgs": args.transferArgs])

    var keys: [AccountMeta] = [
        AccountMeta(
            publicKey: accounts.token,
            isSigner: false,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.tokenOwner,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.destination,
            isSigner: false,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.destinationOwner,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.mint,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.metadata,
            isSigner: false,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.authority,
            isSigner: true,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.payer,
            isSigner: true,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.systemProgram ?? PublicKey.systemProgramId,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.sysvarInstructions,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.splTokenProgram,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.splAtaProgram,
            isSigner: false,
            isWritable: false
        )
    ]
    if accounts.edition != nil {
        
        keys.append(
            AccountMeta(
                publicKey: accounts.edition!,
                isSigner: false,
                isWritable: false
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.ownerTokenRecord != nil {
        if accounts.edition == nil { fatalError("When providing ownerTokenRecord accounts.edition need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.ownerTokenRecord!,
                isSigner: false,
                isWritable: true
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.destinationTokenRecord != nil {
        if accounts.edition == nil || accounts.ownerTokenRecord == nil { fatalError("When providing destinationTokenRecord accounts.edition, accounts.ownerTokenRecord need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.destinationTokenRecord!,
                isSigner: false,
                isWritable: true
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.authorizationRulesProgram != nil {
        if accounts.edition == nil || accounts.ownerTokenRecord == nil || accounts.destinationTokenRecord == nil { fatalError("When providing authorizationRulesProgram accounts.edition, accounts.ownerTokenRecord, accounts.destinationTokenRecord need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.authorizationRulesProgram!,
                isSigner: false,
                isWritable: false
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.authorizationRules != nil {
        if accounts.edition == nil || accounts.ownerTokenRecord == nil || accounts.destinationTokenRecord == nil || accounts.authorizationRulesProgram == nil { fatalError("When providing authorizationRules accounts.edition, accounts.ownerTokenRecord, accounts.destinationTokenRecord, accounts.authorizationRulesProgram need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.authorizationRules!,
                isSigner: false,
                isWritable: false
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }

    let ix = TransactionInstruction(
                keys: keys,
                programId: programId,
                data: data.0.bytes
            )
    return ix
}