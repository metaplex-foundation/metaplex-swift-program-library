/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita-swift
 */
import Foundation
import Solana
import Beet

/**
 * @category Instructions
 * @category FreezeDelegatedAccount
 * @category generated
 */
public struct FreezeDelegatedAccountInstructionArgs{
    let instructionDiscriminator: [UInt8] /* size: 8 */
    

    public init(
        instructionDiscriminator: [UInt8] /* size: 8 */ = freezeDelegatedAccountInstructionDiscriminator
    ) {
        self.instructionDiscriminator = instructionDiscriminator
    }
}
/**
 * @category Instructions
 * @category FreezeDelegatedAccount
 * @category generated
 */
public let FreezeDelegatedAccountStruct = FixableBeetArgsStruct<FreezeDelegatedAccountInstructionArgs>(
    fields: [
        ("instructionDiscriminator", Beet.fixedBeet(.init(value: .collection(UniformFixedSizeArray<UInt8>(element: .init(value: .scalar(u8())), len: 8))))),
        
    ],
    description: "FreezeDelegatedAccountInstructionArgs"
)
/**
* Accounts required by the _FreezeDelegatedAccount_ instruction
*
* @property [_writable_, **signer**] delegate  
* @property [_writable_] tokenAccount  
* @property [] edition  
* @property [] mint   
* @category Instructions
* @category FreezeDelegatedAccount
* @category generated
*/
public struct FreezeDelegatedAccountInstructionAccounts {
    let delegate: PublicKey
    let tokenAccount: PublicKey
    let edition: PublicKey
    let mint: PublicKey
    let tokenProgram: PublicKey?

    public init(
        delegate: PublicKey,
        tokenAccount: PublicKey,
        edition: PublicKey,
        mint: PublicKey,
        tokenProgram: PublicKey? = nil
    ) {
        self.delegate = delegate
        self.tokenAccount = tokenAccount
        self.edition = edition
        self.mint = mint
        self.tokenProgram = tokenProgram
    }
}

public let freezeDelegatedAccountInstructionDiscriminator = [14, 16, 189, 180, 116, 19, 96, 127] as [UInt8]

/**
* Creates a _FreezeDelegatedAccount_ instruction.
*
* @param accounts that will be accessed while the instruction is processed
* @category Instructions
* @category FreezeDelegatedAccount
* @category generated
*/
public func createFreezeDelegatedAccountInstruction(accounts: FreezeDelegatedAccountInstructionAccounts, 
programId: PublicKey=PublicKey(string: "metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s")!) -> TransactionInstruction {

    let data = FreezeDelegatedAccountStruct.serialize(
            instance: ["instructionDiscriminator": freezeDelegatedAccountInstructionDiscriminator ])

    let keys: [AccountMeta] = [
        AccountMeta(
            publicKey: accounts.delegate,
            isSigner: true,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.tokenAccount,
            isSigner: false,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.edition,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.mint,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.tokenProgram ?? PublicKey.tokenProgramId,
            isSigner: false,
            isWritable: false
        )
    ]

    let ix = TransactionInstruction(
                keys: keys,
                programId: programId,
                data: data.0.bytes
            )
    return ix
}