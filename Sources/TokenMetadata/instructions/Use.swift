/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita-swift
 */
import Foundation
import Solana
import Beet

/**
 * @category Instructions
 * @category Use
 * @category generated
 */
public struct UseInstructionArgs{
    let instructionDiscriminator: [UInt8] /* size: 8 */
    let useArgs: UseArgs

    public init(
        instructionDiscriminator: [UInt8] /* size: 8 */ = useInstructionDiscriminator,
        useArgs: UseArgs
    ) {
        self.instructionDiscriminator = instructionDiscriminator
        self.useArgs = useArgs
    }
}
/**
 * @category Instructions
 * @category Use
 * @category generated
 */
public let UseStruct = FixableBeetArgsStruct<UseInstructionArgs>(
    fields: [
        ("instructionDiscriminator", Beet.fixedBeet(.init(value: .collection(UniformFixedSizeArray<UInt8>(element: .init(value: .scalar(u8())), len: 8))))),
        ("useArgs", useArgsBeetWrapped)
    ],
    description: "UseInstructionArgs"
)
/**
* Accounts required by the _Use_ instruction
*
* @property [**signer**] authority  
* @property [_writable_] delegateRecord (optional)  
* @property [_writable_] token (optional)  
* @property [] mint  
* @property [_writable_] metadata  
* @property [_writable_] edition (optional)  
* @property [**signer**] payer  
* @property [] sysvarInstructions  
* @property [] splTokenProgram (optional)  
* @property [] authorizationRulesProgram (optional)  
* @property [] authorizationRules (optional)   
* @category Instructions
* @category Use
* @category generated
*/
public struct UseInstructionAccounts {
    let authority: PublicKey
    let delegateRecord: PublicKey?
    let token: PublicKey?
    let mint: PublicKey
    let metadata: PublicKey
    let edition: PublicKey?
    let payer: PublicKey
    let systemProgram: PublicKey?
    let sysvarInstructions: PublicKey
    let splTokenProgram: PublicKey?
    let authorizationRulesProgram: PublicKey?
    let authorizationRules: PublicKey?

    public init(
        authority: PublicKey,
        delegateRecord: PublicKey? = nil,
        token: PublicKey? = nil,
        mint: PublicKey,
        metadata: PublicKey,
        edition: PublicKey? = nil,
        payer: PublicKey,
        systemProgram: PublicKey? = nil,
        sysvarInstructions: PublicKey,
        splTokenProgram: PublicKey? = nil,
        authorizationRulesProgram: PublicKey? = nil,
        authorizationRules: PublicKey? = nil
    ) {
        self.authority = authority
        self.delegateRecord = delegateRecord
        self.token = token
        self.mint = mint
        self.metadata = metadata
        self.edition = edition
        self.payer = payer
        self.systemProgram = systemProgram
        self.sysvarInstructions = sysvarInstructions
        self.splTokenProgram = splTokenProgram
        self.authorizationRulesProgram = authorizationRulesProgram
        self.authorizationRules = authorizationRules
    }
}

public let useInstructionDiscriminator = [86, 205, 116, 166, 12, 177, 252, 83] as [UInt8]

/**
* Creates a _Use_ instruction.
*
* @param accounts that will be accessed while the instruction is processed
  * @param args to provide as instruction data to the program
 * 
* @category Instructions
* @category Use
* @category generated
*/
public func createUseInstruction(accounts: UseInstructionAccounts, 
args: UseInstructionArgs, programId: PublicKey=PublicKey(string: "metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s")!) -> TransactionInstruction {

    let data = UseStruct.serialize(
            instance: ["instructionDiscriminator": useInstructionDiscriminator,
"useArgs": args.useArgs])

    var keys: [AccountMeta] = [
        AccountMeta(
            publicKey: accounts.authority,
            isSigner: true,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.mint,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.metadata,
            isSigner: false,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.payer,
            isSigner: true,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.systemProgram ?? PublicKey.systemProgramId,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.sysvarInstructions,
            isSigner: false,
            isWritable: false
        )
    ]
    if accounts.delegateRecord != nil {
        
        keys.append(
            AccountMeta(
                publicKey: accounts.delegateRecord!,
                isSigner: false,
                isWritable: true
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.token != nil {
        if accounts.delegateRecord == nil { fatalError("When providing token accounts.delegateRecord need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.token!,
                isSigner: false,
                isWritable: true
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.edition != nil {
        if accounts.delegateRecord == nil || accounts.token == nil { fatalError("When providing edition accounts.delegateRecord, accounts.token need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.edition!,
                isSigner: false,
                isWritable: true
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.splTokenProgram != nil {
        if accounts.delegateRecord == nil || accounts.token == nil || accounts.edition == nil { fatalError("When providing splTokenProgram accounts.delegateRecord, accounts.token, accounts.edition need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.splTokenProgram!,
                isSigner: false,
                isWritable: false
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.authorizationRulesProgram != nil {
        if accounts.delegateRecord == nil || accounts.token == nil || accounts.edition == nil || accounts.splTokenProgram == nil { fatalError("When providing authorizationRulesProgram accounts.delegateRecord, accounts.token, accounts.edition, accounts.splTokenProgram need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.authorizationRulesProgram!,
                isSigner: false,
                isWritable: false
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }
    if accounts.authorizationRules != nil {
        if accounts.delegateRecord == nil || accounts.token == nil || accounts.edition == nil || accounts.splTokenProgram == nil || accounts.authorizationRulesProgram == nil { fatalError("When providing authorizationRules accounts.delegateRecord, accounts.token, accounts.edition, accounts.splTokenProgram, accounts.authorizationRulesProgram need(s) to be provided as well.") }
        keys.append(
            AccountMeta(
                publicKey: accounts.authorizationRules!,
                isSigner: false,
                isWritable: false
            )
        )
    }  else {
        keys.append(
            AccountMeta(
                publicKey: programId,
                isSigner: false,
                isWritable: false
            )
        )
    }

    let ix = TransactionInstruction(
                keys: keys,
                programId: programId,
                data: data.0.bytes
            )
    return ix
}