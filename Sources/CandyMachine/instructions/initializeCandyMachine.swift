/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita-swift
 */
import Foundation
import Beet
import Solana

/**
 * @category Instructions
 * @category InitializeCandyMachine
 * @category generated
 */
public struct InitializeCandyMachineInstructionArgs{
    let instructionDiscriminator: [UInt8] /* size: 8 */
    let data: CandyMachineData

    public init(
        instructionDiscriminator: [UInt8] /* size: 8 */ = initializeCandyMachineInstructionDiscriminator,
        data: CandyMachineData
    ) {
        self.instructionDiscriminator = instructionDiscriminator
        self.data = data
    }
}
/**
 * @category Instructions
 * @category InitializeCandyMachine
 * @category generated
 */
public let initializeCandyMachineStruct = FixableBeetArgsStruct<InitializeCandyMachineInstructionArgs>(
    fields: [
        ("instructionDiscriminator", Beet.fixedBeet(.init(value: .collection(UniformFixedSizeArray<UInt8>(element: .init(value: .scalar(u8())), len: 8))))),
        ("data", candyMachineDataBeetWrapped)
    ],
    description: "InitializeCandyMachineInstructionArgs"
)
/**
* Accounts required by the _initializeCandyMachine_ instruction
*
* @property [_writable_] candyMachine  
* @property [] wallet  
* @property [] authority  
* @property [**signer**] payer   
* @category Instructions
* @category InitializeCandyMachine
* @category generated
*/
public struct InitializeCandyMachineInstructionAccounts {
    let candyMachine: PublicKey
    let wallet: PublicKey
    let authority: PublicKey
    let payer: PublicKey
    let systemProgram: PublicKey?
    let rent: PublicKey?

    public init(
        candyMachine: PublicKey,
        wallet: PublicKey,
        authority: PublicKey,
        payer: PublicKey,
        systemProgram: PublicKey? = nil,
        rent: PublicKey? = nil
    ) {
        self.candyMachine = candyMachine
        self.wallet = wallet
        self.authority = authority
        self.payer = payer
        self.systemProgram = systemProgram
        self.rent = rent
    }
}

public let initializeCandyMachineInstructionDiscriminator = [142, 137, 167, 107, 47, 39, 240, 124] as [UInt8]

/**
* Creates a _InitializeCandyMachine_ instruction.
*
* @param accounts that will be accessed while the instruction is processed
  * @param args to provide as instruction data to the program
 * 
* @category Instructions
* @category InitializeCandyMachine
* @category generated
*/
public func createInitializeCandyMachineInstruction(accounts: InitializeCandyMachineInstructionAccounts, 
args: InitializeCandyMachineInstructionArgs, programId: PublicKey=PublicKey(string: "")!) -> TransactionInstruction {

    let data = initializeCandyMachineStruct.serialize(
            instance: ["instructionDiscriminator": initializeCandyMachineInstructionDiscriminator,
"data": args.data])

    let keys: [AccountMeta] = [
        AccountMeta(
            publicKey: accounts.candyMachine,
            isSigner: false,
            isWritable: true
        ),
        AccountMeta(
            publicKey: accounts.wallet,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.authority,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.payer,
            isSigner: true,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.systemProgram ?? PublicKey.systemProgramId,
            isSigner: false,
            isWritable: false
        ),
        AccountMeta(
            publicKey: accounts.rent ?? PublicKey.sysvarRent,
            isSigner: false,
            isWritable: false
        )
    ]

    let ix = TransactionInstruction(
                keys: keys,
                programId: programId,
                data: data.0.bytes
            )
    return ix
}